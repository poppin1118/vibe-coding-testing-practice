> 狀態：初始為 [ ]、完成為 [x]
> 注意：狀態只能在測試通過後由流程更新。
> 測試類型：前端元素、function 邏輯、Mock API、驗證權限...

---

## [x] 【前端元素】渲染登入表單
**範例輸入**：無
**期待輸出**：畫面包含「電子郵件」輸入框、「密碼」輸入框和「登入」按鈕，且初始狀態無錯誤訊息。

---

## [x] 【前端元素】開發環境顯示測試帳號提示
**範例輸入**：未設定 VITE_API_URL 環境變數
**期待輸出**：畫面下方顯示「測試帳號：任意 email 格式 / 密碼需包含英數且8位以上」提示文字。

---

## [x] 【function 邏輯】驗證 Email 格式錯誤
**範例輸入**：在 Email 欄位輸入「invalid-email」，點擊登入
**期待輸出**：畫面顯示「請輸入有效的 Email 格式」錯誤訊息，不觸發登入 API。

---

## [x] 【function 邏輯】驗證密碼長度不足
**範例輸入**：在 Email 欄位輸入合法 email，在密碼欄位輸入「1234567」，點擊登入
**期待輸出**：畫面顯示「密碼必須至少 8 個字元」錯誤訊息，不觸發登入 API。

---

## [x] 【function 邏輯】驗證密碼未包含英數混合
**範例輸入**：在 Email 欄位輸入合法 email，在密碼欄位輸入「12345678」，點擊登入
**期待輸出**：畫面顯示「密碼必須包含英文字母和數字」錯誤訊息，不觸發登入 API。

---

## [x] 【Mock API】登入成功並導向
**範例輸入**：輸入正確格式 Email 和密碼，點擊登入，Mock login API 回傳成功
**期待輸出**：按鈕顯示「登入中...」及 spinner，成功後清除錯誤訊息，導向至 `/dashboard`。

---

## [x] 【Mock API】登入失敗顯示錯誤
**範例輸入**：輸入正確格式 Email 和密碼，點擊登入，Mock login API 回傳 401 錯誤（如「帳號或密碼錯誤」）
**期待輸出**：畫面上方出現包含 ⚠️ icon 的錯誤提示區塊，顯示 API 回傳的錯誤訊息，且輸入框與按鈕恢復可用狀態。

---

## [x] 【驗證權限】已登入狀態直接導向
**範例輸入**：使用者進入 LoginPage 時，useAuth 的 isAuthenticated 為 true
**期待輸出**：透過 useEffect 自動導向至 `/dashboard` 頁面（replace: true）。

---

## [x] 【驗證權限】顯示授權過期訊息
**範例輸入**：使用者進入 LoginPage 時，useAuth 的 authExpiredMessage 不為空
**期待輸出**：畫面上方顯示對應授權過期錯誤訊息，並呼叫 clearAuthExpiredMessage 方法清空原始紀錄。
